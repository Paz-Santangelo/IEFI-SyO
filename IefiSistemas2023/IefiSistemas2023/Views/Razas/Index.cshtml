@model IEnumerable<IefiSistemas2023.Models.Raza>

@{
    ViewBag.Title = "Razas";
}

<h2>Lista de Razas</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Raza)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaBaja)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Animale.Nombre_Animal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Raza)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaBaja)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Animale.Nombre_Animal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Raza }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Raza }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Raza })
        </td>
    </tr>
}

</table>
